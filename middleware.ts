export { auth as middleware } from "@/auth";
// import { type NextRequest, NextResponse } from "next/server";

// export const middleware = (request: NextRequest) => {
//   console.log("requers: ", request.nextUrl.pathname);
//   return NextResponse.next();
// };
